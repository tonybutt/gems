apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kanidm
resources:
  - ns.yaml
  - certificate.yaml
  - statefulset.yaml
  - service.yaml
  - tlsroute.yaml
configMapGenerator:
  - name: kanidm-config
    files:
      - config.toml
