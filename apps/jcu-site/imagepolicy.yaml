apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: jcu-site
spec:
  imageRepositoryRef:
    name: jcu-site
  filterTags:
    pattern: '^(?P<ts>\d{14})$'
    extract: "$ts"
  policy:
    numerical:
      order: asc
