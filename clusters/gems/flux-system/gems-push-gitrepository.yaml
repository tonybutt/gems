apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: gems-push
  namespace: flux-system
spec:
  interval: 10m
  url: ssh://git@github.com/tonybutt/gems
  ref:
    branch: main
  secretRef:
    name: gems-flux-push
